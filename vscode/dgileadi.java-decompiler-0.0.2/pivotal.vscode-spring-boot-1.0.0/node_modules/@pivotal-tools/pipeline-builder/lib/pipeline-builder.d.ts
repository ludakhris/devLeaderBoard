export interface UserQuestioner {
    (property: string, defaultValue?: string): Promise<string>;
}
export declare function generate_pipeline(projectRoot: string, questioner: UserQuestioner): Promise<void>;
